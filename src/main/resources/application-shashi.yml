server:
  port: 8888
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userpostproject
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE